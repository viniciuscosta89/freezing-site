import { createGlobalStyle } from 'styled-components'

const EmpresaStyled = createGlobalStyle`
  .section {
    margin-bottom: 4rem;
  }

  p {
    text-align: justify;
  }

  .info {
    .img-fluid {
      width: 100%;
    }
  }
`

export default EmpresaStyled;
