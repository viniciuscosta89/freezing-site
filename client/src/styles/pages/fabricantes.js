import { createGlobalStyle } from 'styled-components'

const FabricantesStyled = createGlobalStyle`
  .section {
    margin-bottom: 2.5rem;
  }

  .description {
    color: var(--primary-color);
    text-align: justify;
  }
`

export default FabricantesStyled;
